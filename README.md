Frontend - HTTP, CSS and JS
Backend - Flask (can run both as a code or inside a container) and mariadb (container only)

Environment attr (.env) example:
  DB_USER=user
  DB_PASSWORD=password
  DB_HOST=localhost
  DB_NAME=example
  DB_ROOT=rootpassword
